# 1A2B 遊戲規則

1A2B 是一款單機猜數字益智遊戲。每一題由電腦設定答案，玩家根據每次猜測得到的提示，推理出電腦藏起來的數字。

## 一、遊戲目標

猜出一組由 **4 個不重複數字** 組成的答案。當提示達到 **4A0B** 時，代表答案完全正確。

本文件採用常見規則：數字從 0–9 中選取，且 **0 可以出現在第一位**。若遊戲事先約定首位不可為 0，則依該遊戲設定為準。

## 二、A 與 B 的意思

- **A（完全命中）**：數字正確，而且位置也正確。
- **B（部分命中）**：數字正確，但位置不正確。
- 不在答案中的數字，不會計入 A 或 B。

例如答案是 `4271`，猜測 `1234`：

- `2` 位於第二位，完全正確，得到 **1A**。
- `1` 與 `4` 都在答案中，但位置錯誤，得到 **2B**。
- `3` 不在答案中，不計算。

因此提示為：

```text
1A2B
```

## 三、單機遊戲流程

1. 遊戲開始時，電腦隨機產生一組 4 位、不重複的數字，並保密答案。
2. 玩家提出一組符合規則的 4 位數字。
3. 電腦比對猜測與答案，回覆 A 與 B 的數量，並將這次回答加入畫面上的回答紀錄。
4. 玩家根據紀錄與提示調整下一次猜測。
5. 重複猜測，直到得到 `4A0B`。
6. 玩家答對後，電腦重新產生一題，並清空上一題的回答紀錄。

## 四、判定方式

### 1. 先計算 A

逐位比較猜測與答案；數字和位置都相同的，每個計為 1A。

### 2. 再計算 B

在扣除已計入 A 的數字後，若猜測中的數字出現在答案的其他位置，每個計為 1B。

### 3. 每個數字最多計算一次

同一個數字不會重複計入。標準玩法要求答案與猜測都不能有重複數字，因此通常不會遇到重複計算問題。

## 五、猜測限制

- 每次猜測必須是 4 位數字。
- 答案與猜測都不得含有重複數字。
- 若採用本文件的標準版本，數字可使用 0–9，且首位可以是 0。
- 已經猜過的組合可以由遊戲自行禁止，或由玩家自行記錄避免重複。

## 六、勝負條件

### 猜中答案

得到 `4A0B` 即代表 4 個數字與位置全部正確，猜題者獲勝。

### 用完次數

若遊戲設定猜測次數上限，猜測次數用完仍未得到 `4A0B`，則由電腦公布答案，玩家落敗。

若沒有設定次數上限，則可以持續猜測，直到得到 `4A0B`。

## 七、完整示例

假設答案為：

```text
4271
```

| 猜測 | 結果 | 說明 |
| --- | --- | --- |
| `1234` | `1A2B` | `2` 位置正確；`1`、`4` 數字正確但位置錯誤 |
| `4872` | `2A1B` | `4`、`7` 位置正確；`2` 數字正確但位置錯誤 |
| `4271` | `4A0B` | 4 個數字與位置全部正確 |

## 八、常見誤解

- `1A2B` 不是代表猜對了 1 個數字、猜錯了 2 個數字，而是代表 1 個數字位置也正確、另外 2 個數字只猜中數字。
- A 與 B 的總數不一定等於 4，沒有出現在答案中的數字不會計入。
- `4A0B` 才是完整猜中；`0A4B` 代表 4 個數字都猜中，但位置全部錯誤。
- 回報提示時只回覆 A、B 數量，不應透露是哪幾個數字或哪個位置。
